{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% if object %}
        {% set p = get_predefined_key(object.storageKey) %}
        {% if p %}
            {{ p.friendlyName|raw }}
        {% endif %}
    {% endif %}
{% endblock %}