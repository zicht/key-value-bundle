{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% if object %}
        <span title="{{ object.storageKey }}">{{ object.storageKey|split('.')|last }}</span>
    {% endif %}
{% endblock %}